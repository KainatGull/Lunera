import {useMemo,useState} from 'react';import {useSearchParams} from 'react-router-dom';import Header from '../components/Header';import Footer from '../components/Footer';import ProductCard from '../components/ProductCard';import Toast from '../components/Toast';import {products} from '../data';
export default function Shop(){const [params]=useSearchParams();const initial=params.get('category')||'All';const [category,setCategory]=useState(initial);const [search,setSearch]=useState(params.get('search')||'');const [toast,setToast]=useState('');const cats=['All','Face','Body','Ritual Sets'];const filtered=useMemo(()=>products.filter(p=>(category==='All'||p.category===category)&&(!search||`${p.name} ${p.description}`.toLowerCase().includes(search.toLowerCase()))),[category,search]);return <><Header/><main className="shop-page"><div className="shell"><div className="shop-heading"><div><p className="eyebrow">THE COLLECTION</p><h1>Everyday rituals,<br/><em>considered.</em></h1></div><p>Simple, plant-minded essentials for the skin you live in.</p></div><div className="shop-controls"><div className="category-tabs">{cats.map(c=><button key={c} className={category===c?'active':''} onClick={()=>setCategory(c)}>{c}</button>)}</div><input aria-label="Search products" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search..."/></div>{filtered.length?<div className="product-grid">{filtered.map(p=><ProductCard key={p.id} product={p} onToast={setToast}/>)}</div>:<section className="empty-state"><div>✦</div><h2>Nothing in this patch.</h2><p>Try a different search or return to all rituals.</p><button onClick={()=>{setSearch('');setCategory('All')}}>Reset search</button></section>}</div></main><Footer/><Toast message={toast} onDone={()=>setToast('')}/></>}